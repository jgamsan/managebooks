<% content_for :content do %>
  <h2>Servicios Disponibles en <%= @resort.name.capitalize %></h2>

    <table class="zebra-striped">
      <tr>
        <th>Nombre</th>
        <th>Disponible</th>
        <th>Duracion</th>
        <th>Coste</th>
        <th></th>
        <th></th>
      </tr>
      <% for offer in @offers %>
        <tr>
          <td><%= offer.service_extra.name %></td>
          <td><%= offer.enable %></td>
          <td><%= offer.hduration %></td>
          <td><%= offer.hcost %></td>
          <td><%= link_to "Edit", edit_admin_resort_offer_path(@resort, offer) %></td>
          <td><%#= link_to "Destroy", admin_category_path(category), :confirm => 'Are you sure?', :method => :delete %></td>
        </tr>
      <% end %>
    </table>
    <%#= will_paginate @products %>
    <br />
    <br />
    <p><%#= link_to "Nuevo Categoria", new_admin_category_path %></p>

<% end %>

