# Use the README file as a reference to customize settings.

Identity:
  fields:
    default: name, email, password_digest
    form: name, email, password_digest
  relationships: 
  application: Application
