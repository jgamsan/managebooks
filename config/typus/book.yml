# Use the README file as a reference to customize settings.

Book:
  fields:
    default: user, day, interval
    list: user, day, interval
    form: user, day, interval
  relationships: interval, user
  filters: day
  application: Application
